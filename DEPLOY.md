# GitHub Pages 部署指南

## 🚀 快速部署步骤

### 1. 提交代码到 GitHub

```bash
# 添加所有文件
git add .

# 提交更改
git commit -m "Add GitHub Pages deployment configuration"

# 推送到 GitHub
git push origin main
```

### 2. 启用 GitHub Pages

1. 打开 GitHub 仓库：https://github.com/siegzhong-maker/the-safe-house
2. 进入 **Settings**（设置）
3. 在左侧菜单中找到 **Pages**
4. 在 **Source** 部分，选择 **GitHub Actions**
5. 保存设置

### 3. 触发部署

部署会在以下情况自动触发：
- ✅ 推送到 `main` 分支时自动部署
- ✅ 推送到 `master` 分支时自动部署
- ✅ 手动在 Actions 页面触发 workflow

### 4. 查看部署状态

1. 进入仓库的 **Actions** 标签页
2. 查看最新的 workflow 运行状态
3. 等待部署完成（通常需要 1-2 分钟）

### 5. 访问你的网站

部署成功后，你的网站将在以下地址可用：
**https://siegzhong-maker.github.io/the-safe-house/**

## 📝 配置文件说明

- `.github/workflows/deploy.yml` - GitHub Actions 自动部署配置
- `.nojekyll` - 禁用 Jekyll 处理，确保所有文件正常显示
- `vite.config.ts` - 已配置 `base: '/the-safe-house/'` 路径

## ⚠️ 注意事项

1. **Base Path**: 确保 `vite.config.ts` 中的 `base` 路径与你的仓库名匹配
2. **首次部署**: 首次部署可能需要几分钟时间
3. **更新内容**: 每次推送到 main 分支都会自动重新部署

## 🔧 故障排除

如果部署失败：
1. 检查 Actions 页面的错误日志
2. 确认 Node.js 版本兼容（当前配置为 Node 20）
3. 确认 `package.json` 中的构建脚本正确
4. 检查 GitHub Pages 设置中是否选择了 "GitHub Actions" 作为源

